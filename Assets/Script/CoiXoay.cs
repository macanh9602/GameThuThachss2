using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;
public class CoiXoay : MonoBehaviour
{
    [SerializeField] Transform[] target;

    // Start is called before the first frame update
    void Start()
    {
        //update
    }

    // Update is called once per frame
    void Update()
    {

    }
}
